for code review

QS1：

兩種不同遞迴式（有無回傳值）的寫法區別而已。


QS2：

#########################

這邊預設入的測資都已經將多項式合併，但是並未排序過。
所以測資不會出現
2x^3 + 4x + 5x^3 +1 這種多項式，
預設會被合併成為
7x^3 + 4x + 1 這種類型，
但是不需要被排序過。

#########################

(a).
這邊寫了兩種邏輯，但是概念都一樣
先遍歷要比較的對象，尋找第一項，找不到直接false
再來記住第一項後直接開始比較，
剩下的項有沒有完全相同，沒有就false掉

(b).
這邊就不寫兩個只寫Midterm202511.cpp中的判斷邏輯了，
更簡潔的寫法，找到第一項後直接接著判斷，找不到就false掉。

(c). (d).
Array、Linkedlist寫法邏輯上一樣
將第一項存入Max當中，然後遍歷容器後逐項比對最大值。


QS3.

(a). (b).
Array、Linkedlist寫法邏輯上一樣，
雙重loop逐項比較other的每一項的內容，我有沒有存在一份就好，
沒有再false掉。

(c). (d).
Array、Linkedlist寫法邏輯上一樣，
先比自己跟otherbag的itemcount大小，
不同的可以直接false掉不用浪費時間了。
接下來都直接用vector複製一份自己還有otherbag出來，
然後將這兩個vector重新sort這邊用的是InsertSort，
排序後再依次比較，有不同就可以直接false掉了。
